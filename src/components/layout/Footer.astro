---
import { menuLinks } from "@/site-config";
---

<footer
	class="mt-auto flex w-full flex-col items-center justify-center gap-y-4 pb-4 pt-20 text-center align-top font-semibold text-gray-600 dark:text-gray-400 sm:flex-row sm:justify-between sm:gap-y-0 sm:text-xs"
>
	<div class="me-0 sm:me-4">
		<span class="inline-block">🐄 Cowsay.farm</span>
	</div>
	<nav
		aria-label="More on this site"
		class="flex flex-wrap items-center justify-center gap-x-3 gap-y-2 sm:gap-x-0 sm:gap-y-0 sm:divide-x sm:divide-gray-500"
	>
		{
			menuLinks.map((link) => (
				<a class="px-2 py-2 sm:px-4 sm:py-0 sm:hover:text-textColor sm:hover:underline" href={link.path}>
					{link.title}
				</a>
			))
		}
	</nav>
</footer>
